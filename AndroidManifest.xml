<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.webileapps.navdrawer"
    android:versionCode="3"
    android:versionName="1.02" >

    <uses-permission android:name="android.permission.INTERNET" />

    <android:uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" /> <!-- android:targetSdkVersion="17" -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/fup"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sherlock" >
        <activity
            android:name="com.webileapps.navdrawer.FmLogin"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.webileapps.navdrawer.MainActivity"
            android:label="@string/title_activity_myActivity">
        </activity>

    </application>

</manifest>